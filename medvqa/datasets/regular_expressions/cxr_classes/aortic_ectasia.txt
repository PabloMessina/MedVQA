
    \b(?:
        # 1. Adjective First
        (?:
            ectasi\w*|ectatic|dilat\w*|dilation|dilatation
        )
        (?:
            \W+\w+
        ){0,4}
        \s+
        (?:
            aort\w*|arch|root
        )

        |

        # 2. Anatomy First
        (?:
            aort\w*|arch|root
        )
        (?:
            \W+\w+
        ){0,4}
        \s+
        (?:
            ectasi\w*|ectatic|dilat\w*
        )

        |

        # 3. Noun-form
        (?:
            ectasia|dilation|dilatation
        )
        \s+(?:of|in)\s+(?:the\s+)?
        (?:
            thoracic\s+|
            ascending\s+|
            descending\s+
        )?
        (?:
            aort\w*|arch|root
        )
    )\b
    